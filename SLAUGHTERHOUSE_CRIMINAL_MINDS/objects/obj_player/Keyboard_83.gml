/// @description Insert description here
// You can write your code in this editor
if(canAttack == true) && qtimes > 2
{
if (!place_meeting(x,y+2,obj_wall))
	{
		
		if keyboard_check(ord("S"))
		{
			
			y+= 1;
			sprite_index = spr_player_down
				soundStep = snd_step2
			if (sprite_index == spr_player_down and round(image_index) =3 && !audio_is_playing(soundStep)){
audio_play_sound(soundStep,10,false);

}
if (sprite_index == spr_player_down and round(image_index) =0 && !audio_is_playing(soundStep)){
audio_play_sound(soundStep,10,false);
}
		}
	}
	
}