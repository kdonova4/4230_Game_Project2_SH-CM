/// @description Insert description here
// You can write your code in this editor
if (matches > 0 && lightOn = false)
{
	audio_play_sound(snd_torchLight, 1, false)
	lightOn = true;
	torchLife = torchTimer
	matches -= 1;
}

