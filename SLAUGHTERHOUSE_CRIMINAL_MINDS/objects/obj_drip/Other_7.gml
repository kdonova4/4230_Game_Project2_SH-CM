/// @description Insert description here
// You can write your code in this editor
if distance_to_object(obj_player) < 100
{
	sound = audio_play_sound(snd_drip1, 1, false);
audio_sound_gain(sound, volume, 1)
}
